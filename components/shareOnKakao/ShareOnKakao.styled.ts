import styled from 'styled-components';

export const StyleShareWrapper = styled.div(() => ({
  marginTop: '60px',
  padding: '30px 0',
  background: '#f1965f12',
  lineHeight: '2rem',
}));

export const StyleKakao = styled.div(() => ({
  margin: '0px auto',
  width: '300px',
  cursor: 'pointer',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
}));

export const StyleLink = styled.div(() => ({
  margin: '0px auto',
  width: '300px',
  cursor: 'pointer',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
}));

export const StyleKaKaoLogo = styled.img(() => ({
  width: 30,
  height: 30,
}));

export const StyleCopyLogo = styled.img(() => ({
  width: 20,
  height: 20,
}));

export const StyleText = styled.span(() => ({
  width: '11rem',
  marginLeft: 10,
}));
